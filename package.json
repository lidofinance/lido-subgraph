{
  "name": "lido-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ lidofinance/lido",
    "create-local": "env-cmd --use-shell graph create --node '$GRAPH_NODE' lido",
    "remove-local": "env-cmd --use-shell graph remove --node '$GRAPH_NODE' lido",
    "deploy-local": "env-cmd --use-shell graph deploy --node '$GRAPH_NODE' --ipfs '$GRAPH_IPFS' lido"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.1"
    "env-cmd": "^10.1.0",
  }
}
