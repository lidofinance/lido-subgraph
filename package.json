{
  "name": "lido-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ lidofinance/lido",
    "create-local": "graph create --node http://localhost:8020/ lido",
    "remove-local": "graph remove --node http://localhost:8020/ lido",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lido"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.1"
  }
}
